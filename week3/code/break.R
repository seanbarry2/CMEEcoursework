i <- 0 #Initialize i
    while (i < Inf) {
        if (i == 10) {
            break # Break out of the loop when `i` reaches 10
        } else { # Break out of the while loop!  
            cat("i equals " , i , " \n")
            i <- i + 1 # Increment i by 1
    }
}
